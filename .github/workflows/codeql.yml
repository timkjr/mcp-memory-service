name: "CodeQL Security Analysis"

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    # Run weekly security scans on Mondays at 00:00 UTC
    - cron: '0 0 * * 1'

jobs:
  analyze:
    if: github.repository == "doobidoo/mcp-memory-service"
    name: Analyze Python Code
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      # Required for CodeQL to upload results
      security-events: write
      # Required to fetch internal or private CodeQL packs
      packages: read
      # Required for private repositories
      contents: read
      actions: read

    strategy:
      fail-fast: false
      matrix:
        language: [ 'python' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

    - name: Set up Python
      uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
      with:
        python-version: '3.11'

    # Install dependencies to improve CodeQL analysis accuracy
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[sqlite]"

    # Initialize CodeQL tools for scanning
    - name: Initialize CodeQL
      uses: github/codeql-action/init@7211b7c8077ea37d8641b6271f6a365a22a5fbfa # v4.36.0
      with:
        languages: ${{ matrix.language }}
        # Query suites: security-extended includes more security checks
        queries: security-and-quality
        # Custom CodeQL config for excluding test files, etc.
        config-file: ./.github/codeql/codeql-config.yml

    # Autobuild attempts to build any compiled languages (not needed for Python)
    # Python analysis works by scanning the source code directly
    - name: Autobuild
      uses: github/codeql-action/autobuild@7211b7c8077ea37d8641b6271f6a365a22a5fbfa # v4.36.0

    # Perform CodeQL Analysis
    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@7211b7c8077ea37d8641b6271f6a365a22a5fbfa # v4.36.0
      with:
        category: "/language:${{matrix.language}}"
        # Upload SARIF file for GitHub Security tab
        upload: true

    # Optional: Fail build if high-severity issues found
    # Uncomment to enforce security gates
    # - name: Check for high-severity findings
    #   run: |
    #     if [ -f "codeql-results.sarif" ]; then
    #       HIGH_COUNT=$(jq '[.runs[].results[] | select(.level == "error")] | length' codeql-results.sarif)
    #       if [ "$HIGH_COUNT" -gt 0 ]; then
    #         echo "❌ Found $HIGH_COUNT high-severity security issues"
    #         exit 1
    #       fi
    #     fi
