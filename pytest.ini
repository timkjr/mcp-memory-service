[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Use prepend import mode to ensure package is properly imported
addopts = --import-mode=prepend
markers =
    unit: unit tests
    integration: integration tests
    performance: performance tests
    asyncio: mark test as async
    enable_plugins: allow plugin discovery in test (overrides _disable_plugins fixture)
